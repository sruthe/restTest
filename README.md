# restTest

- To Install dependencies:

```bash
$ npm install
```

- To Launch the server:

```bash
$ npm start
```
Now, the application will run at [http://localhost:3000] and the Transactions per day and Running Total will be displayed on Server console.

- To Run the tests:

```bash
$ npm run test
```
